import { Screen } from '@/design/layout'
import { P } from '@/design/text'
import { memo } from 'react'

const ExploreScreen = memo(function ExploreScreen() {
  return (
    <Screen>
      <P color="primary">Explore</P>
    </Screen>
  )
})

export default ExploreScreen
